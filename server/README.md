# Server

Not built yet. The client currently reads mock data through
`client/src/api/catalog.js`.

Planned: an API over PostgreSQL on Amazon RDS (single region first), serving
the catalog plus auth and profiles. Framework choice TBD.
